# Dashboard

This directory will hold dependency-free HTML, CSS, and TypeScript assets for the
local management dashboard. Start with native browser APIs; introduce a frontend
framework only if the dashboard develops complex client-side state, routing, or
reusable interactive views.
